package training.scala

case class SimpleMetric (
                        device: String,
                        metric: String,
                        value: Double,
                        unit: String
                        ) 
